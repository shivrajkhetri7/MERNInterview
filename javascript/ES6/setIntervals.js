/**
 * setInterval  run the function in every iteration certain period in time
 */

const Content = "THIS is set time interval";

setInterval(() => {
  console.log(Content);
}, 2000);

/**
 * Output 
 * 
THIS is set time interval
THIS is set time interval
THIS is set time interval
THIS is set time interval
THIS is set time interval
THIS is set time interval
.
.
.
 */
